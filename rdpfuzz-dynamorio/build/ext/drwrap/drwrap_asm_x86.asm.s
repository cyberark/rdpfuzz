





























































































































































































































































































































































































































































































































































































































































































































































































































































































 
 
 
.CODE


EXTERN replace_native_xfer_stack_adjust:PROC
EXTERN replace_native_xfer_app_retaddr:PROC
EXTERN replace_native_xfer_target:PROC








        replace_native_xfer PROC

        












        push     0 
        push     rax
        push     rdx
        lea rsp, [-32 - 8*0 + rsp] 
 call replace_native_xfer_stack_adjust 
 lea rsp, [32 + 8*0 + rsp]
        mov      ecx, eax
        pop      rdx
        pop      rax
        lea      rsp, [ 8 + rsp ]
        



        mov      qword ptr [-2 * 8 + rsp], 0
        mov      qword ptr [-1 * 8 + rsp], 0

        
        sub      rsp, rcx
        
        push     rax

        push     rax
        push     rdx

        



        push     rbx
        mov      rbx, rsp
        and      rsp, -16 

        
        lea rsp, [-32 - 8*0 + rsp] 
 call replace_native_xfer_app_retaddr 
 lea rsp, [32 + 8*0 + rsp]
        mov      qword ptr [3 * 8 + rbx], rax

        
        lea rsp, [-32 - 8*0 + rsp] 
 call replace_native_xfer_target 
 lea rsp, [32 + 8*0 + rsp]
        mov      rcx, rax

        mov      rsp, rbx
        pop      rbx

        pop      rdx
        pop      rax
        



        mov      qword ptr [-2 * 8 + rsp], 0
        mov      qword ptr [-1 * 8 + rsp], 0
        jmp      rcx
        
        ret
        replace_native_xfer ENDP









PUBLIC replace_native_ret_imms
PUBLIC replace_native_ret_imms_end

        replace_native_rets PROC

        ret
replace_native_ret_imms::
        ret      1 * 8
        ret      2 * 8
        ret      3 * 8
        ret      4 * 8
        ret      5 * 8
        ret      6 * 8
        ret      7 * 8
        ret      8 * 8
        ret      9 * 8
        ret      10 * 8
        ret      11 * 8
        ret      12 * 8
        ret      13 * 8
        ret      14 * 8
        ret      15 * 8
        ret      16 * 8
        ret      17 * 8
        ret      18 * 8
        ret      19 * 8
        ret      20 * 8
        ret      21 * 8
        ret      22 * 8
        ret      23 * 8
        ret      24 * 8
        ret      25 * 8
        ret      26 * 8
        ret      27 * 8
        ret      28 * 8
        ret      29 * 8
        ret      30 * 8
        ret      31 * 8
        ret      32 * 8
        ret      33 * 8
        ret      34 * 8
        ret      35 * 8
        ret      36 * 8
        ret      37 * 8
        ret      38 * 8
        ret      39 * 8
        ret      40 * 8
replace_native_ret_imms_end::
        nop
        replace_native_rets ENDP







        replace_retaddr_sentinel PROC

        ret
        replace_retaddr_sentinel ENDP


END
